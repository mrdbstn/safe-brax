# Safe-Brax Requirements
# Python >= 3.10

# Core JAX stack
jax==0.6.0
jaxlib==0.6.0
jax-cuda12-plugin==0.6.0  # For GPU support on CUDA 12.x
jax-cuda12-pjrt==0.6.0

# Brax core dependencies
absl-py>=2.3.0
etils>=1.13.0
flax>=0.10.0
jaxopt>=0.8.0
ml_collections>=1.1.0
numpy>=2.2.0
optax>=0.2.0
orbax-checkpoint
chex>=0.1.89

# MuJoCo (physics engine) - IMPORTANT: Keep these versions synchronized!
mujoco==3.3.2
mujoco-mjx==3.3.2

# Training and logging
wandb>=0.19.0
tensorboardX

# Visualization and plotting
matplotlib>=3.10.0
imageio>=2.37.0
Pillow

# Web server (for visualization)
flask>=3.1.0
flask-cors>=6.0.0
jinja2

# Utilities
scipy
trimesh
typing-extensions

# Optional: Safety Gymnasium (if using safety-gym environments)
# Note: Has version conflicts with mujoco 3.3.2, but can be ignored for brax environments
safety-gymnasium==1.0.0

# Optional: Gym compatibility
gym>=0.26.0
gymnasium>=0.28.0

# Development tools (optional)
pytest
ipython
jupyter

